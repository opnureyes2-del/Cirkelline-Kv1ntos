apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: cirkelline-backend-pdb
  namespace: cirkelline
  labels:
    app: cirkelline-backend
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: cirkelline-backend
